{
  "scripts": {
    "deploy": "./deploy.sh",
    "delete": "aws cloudformation delete-stack --stack-name sustaintracker"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.41.0",
    "aws-amplify": "^4.3.7",
    "aws-sdk": "^2.1032.0",
    "node-fetch": "^3.1.0"
  }
}
